# The DEFAULT registry, stated rather than inherited. Everything here resolves from and publishes to
# npmjs at `access: public`; no auth is needed to install anything in this repository.
registry=https://registry.npmjs.org/
